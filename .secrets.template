# Template for act secrets file
# Copy this to .secrets and fill in your actual values
# NEVER commit the .secrets file!

# GitHub token for actions that need authentication
GITHUB_TOKEN=your_github_token_here

# PyPI token for publishing (if needed)
PYPI_API_TOKEN=your_pypi_token_here

# Other secrets as needed
# SECRET_NAME=secret_value